<h2 class="h1-responsive font-weight-bold my-5">Connexion</h2>
<body>
   <div class=" border border-light p-5">
     <p class="h4 mb-4">Pour vous connecter, compléter le formulaire</p>
      <%= form_tag url_for(action: 'create'), method: "post" do %>

        <p><%= label_tag 'Email' %></p>
        <%= text_field_tag(:email), class: "form-control mb-2"%>
        <p><%= label_tag 'Mot de passe' %></p>
        <%= text_field_tag(:pasword) class: "form-control mb-2"%>
           </br>
        <%= submit_tag "Connexion", class: "btn btn-info btn-block" %>
      <% end %>
    </div>
</body>