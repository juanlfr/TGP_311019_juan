
<h2 class="h1-responsive font-weight-bold my-5">Page de création d'un nouveau gossip</h2>

<body>

	<div class=" border border-light p-5">

	  <p class="h4 mb-4"> Pour créer un nouveau gossip, remplissez le formulaire ci-dessous </p>

	  
	    <%= form_tag url_for(action: 'create'), method: "post" do %>

					<% if @gossip.errors.any? %>
							<div class="alert alert-danger" role="alert">
								  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
									  <ul>
									    <% @gossip.errors.full_messages.each do |message| %>
									      <li><%= message %></li>
									    <% end %>
									  </ul>
									<% end %>			
							</div>

				<div style="margin-left: 60px">
		      <%= label_tag 'Titre du gossip :' %>
		      <%= text_field_tag(:title)%>
		      <br /> <br />

		      <%= label_tag 'Dîtes-nous tout :' %>
		      <%= text_field_tag(:content)%>
		      <br /> <br />
		  
		      <%= submit_tag "Valider", class: "btn btn-info btn" %>
				</div>

	    <% end %>

	  

	</div>

</body>
